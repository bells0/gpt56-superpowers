interface:
  display_name: "GPT-5.6 Verification"
  short_description: "Choose evidence for unclear validation"
  default_prompt: "Use $gpt56-verification to select proportionate evidence for this change."
policy:
  allow_implicit_invocation: true
