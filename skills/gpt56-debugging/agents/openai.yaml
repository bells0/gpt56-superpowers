interface:
  display_name: "GPT-5.6 Debugging"
  short_description: "Investigate ambiguous or persistent failures"
  default_prompt: "Use $gpt56-debugging to diagnose this failure from evidence."
policy:
  allow_implicit_invocation: true
