interface:
  display_name: "GPT-5.6 Design & Planning"
  short_description: "Resolve material design and scope ambiguity"
  default_prompt: "Use $gpt56-design-planning to resolve the material design choices in this task."
policy:
  allow_implicit_invocation: true
