interface:
  display_name: "GPT-5.6 Git & Delivery"
  short_description: "Commit completed changes and handle Git delivery"
  default_prompt: "Use $gpt56-git-delivery to safely finish this repository change with its scoped local commit and any authorized delivery."
policy:
  allow_implicit_invocation: true
