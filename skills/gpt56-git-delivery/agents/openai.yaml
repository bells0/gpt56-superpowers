interface:
  display_name: "GPT-5.6 Git & Delivery"
  short_description: "Handle branches, commits, PRs, and cleanup"
  default_prompt: "Use $gpt56-git-delivery to complete this authorized Git delivery task."
policy:
  allow_implicit_invocation: true
